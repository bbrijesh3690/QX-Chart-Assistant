# QX Chart Assistant (v1.2.6-analysis)

A read-only multi-timeframe analysis assistant for Quotex.

## Features
- Captures visible chart candles instantly via WebSocket.
- Auto-locks onto live asset price.
- Preserves history across page refreshes via `sessionStorage`.
